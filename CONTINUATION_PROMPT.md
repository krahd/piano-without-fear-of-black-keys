Resume *Piano Without Fear of Black Keys* as a standalone repository project. Read `STATUS.md`, then `PLAN.md`, then the latest `WORKLOG.md`. Treat actual GitHub remote state as canonical published state and the Mac checkout/workspaces as authoritative local state. Use the Mac Git Bridge for all Mac filesystem/shell/Git work. Preserve `krahd/musifold` as the interactive application and `krahd/ableton-note-helper` as its narrower predecessor; do not merge the experimental Musifold book branch into `main`. Reconcile any existing `krahd/piano-without-fear-of-black-keys` remote/local state before mutation. Work in microsteps, use the workspace coordinator for substantial edits, checkpoint/push coherent work, build and preflight the PDF, render representative pages, integrate through the workspace gate, and independently verify the final GitHub ref/content. If a command or delivery outcome is ambiguous, inspect actual state before retrying. Completion requires the standalone repo on GitHub `main`, passing build/QA, accurate persistent state, and no accidental piano-book containment in Musifold `main`.
